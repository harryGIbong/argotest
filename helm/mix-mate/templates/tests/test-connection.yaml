apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mix-mate.fullname" . }}-test-connection"
  labels:
    {{- include "mix-mate.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mix-mate.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
